二分法查找
mid = (low+high)/2; will cause Runtime Error!!!!!!!(OVERFLOW)

when start and end are all about INT_MAX , then (start+end) of course will be overflow !
To avoid the problem:
mid = low + (high-low)/2;
